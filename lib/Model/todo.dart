class Todo {
  var id;
  var name;
  var owner_name;
  var owner_image;
  var parent;
  var project_id;
  var start_date;
  var due_date;
  var status;

  Todo(this.due_date, this.id, this.name, this.owner_image, this.owner_name,
      this.parent, this.project_id, this.start_date, this.status);
}
